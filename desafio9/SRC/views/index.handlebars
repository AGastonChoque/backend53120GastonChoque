<div class="p-3 m-2 container d-flex flex-column align-items-center">
    <h1>Bienvenido, iniciaste sesion como:</h1>
    {{#if user.username}}
       <h3>
        Username: {{user.username}} <br>
        Nombre: {{user.complete_name}} <br>
        email: {{user.email}}
    </h3>
    {{else}}
       <h3>
        Nombre: {{user.first_name}} <br>
        Apellido: {{user.last_name}} <br>
        edad: {{user.age}} <br>
        email: {{user.email}}
    </h3>
    {{/if}}
    
</div>